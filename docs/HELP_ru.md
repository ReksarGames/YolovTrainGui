# 📖 **Руководство по конфигурации**

<p align="center">
  <a href="HELP.md">🇺🇸 English</a> | <a href="HELP_ru.md">🇷🇺 <b>Русский</b></a>
</p>

> Полное объяснение всех конфигурационных файлов YDS, их структуры и использования.

---

## 📑 **Быстрая навигация**

| Раздел | Тема |
|--------|------|
| [⚙️ Расположение файлов](#-расположение-конфигурационных-файлов) | Где находятся конфиги |
| [🔧 Конфиг #1](#-конфиг-1-configsconfigjson--основная-конфигурация) | Основные настройки (GUI, данные, обучение) |
| [🎬 Конфиг #2](#-конфиг-2-configsconfigstreamcutjson--streamcut) | StreamCut обработка VOD |
| [🎯 Примеры](#-пошаговые-примеры-конфигурации) | Реальные сценарии |
| [🔧 Отладка](#-контрольный-список-отладки) | Решение проблем |
| [📊 Производительность](#-оптимизация-производительности) | Советы по скорости |
| [🎨 YOLO модели](#-работа-с-разными-yolo-моделями) | Выбор и использование |
| [💾 Экспорт](#-экспорт-и-развертывание-моделей) | Сохранение моделей |

---

## 📂 **Расположение конфигурационных файлов**

```
YolovTrainGui/
├── configs/
│   ├── config.json           # ⚙️ Основной конфиг (GUI, сбор данных, обучение)
│   └── configStreamCut.json  # 🎬 Конфиг StreamCut (обработка VOD)
```

**💡 Полезно знать:** Большинство параметров можно менять в GUI, но конфигурационные файлы обеспечивают:
- ✅ Точный контроль всех настроек
- ✅ Сохранение пресетов между сеансами
- ✅ Запуск скриптов напрямую из CLI

---

## � **Конфиг #1: `configs/config.json`** — Основная конфигурация

**Используется для:**
- 📸 **Data Collection** — Параметры полуавтоматического захвата экрана
- 🖌️ **Label Tool** — Верификация и корректировка разметки
- 📂 **Dataset Splitting** — Разбиение на train/val/test
- ⚙️ **Training** — Значения гиперпараметров по умолчанию
- 🎨 **UI** — Внешний вид интерфейса

<details>
<summary><b>📋 Нажмите чтобы развернуть JSON пример</b></summary>

### **Основные параметры**

```json
{
  "model_path": "models/sunxds_0.7.6.pt",
  "classes": ["player", "head", "weapon"],
  
  "grabber": {
    "crop_size": 0.8,
    "width": 640,
    "height": 640
  },
  
  "output_folder": "dataset_output",
  "save_interval": 3,
  "detection_threshold": 0.5,
  
  "train_defaults": {
    "epochs": 50,
    "imgsz": 640,
    "batch": 16,
    "optimizer": "SGD",
    "lr0": 0.01,
    "patience": 15,
    "mosaic": 1.0,
    "mixup": 0.1,
    "amp": true
  },
  
  "ui": {
    "font_size": 10,
    "theme": "light_cyan"
  }
}
```

</details>

### **Объяснение параметров**

#### **📸 Настройки сбора данных**
- **model_path** — YOLO модель для детекции (влияет только на сбор данных, не на обучение)
- **classes** — Объекты для захвата (порядок = ID класса в датасете)
- **grabber.crop_size** — Область экрана для захвата (0.8 = 80% центра)
- **grabber.width/height** — Фиксированное разрешение захвата
- **detection_threshold** — Мин. уверенность для сохранения кадра (0-1)
- **save_interval** — Секунды между сохранениями (предотвращает дубликаты)

#### **📂 Выход и хранилище**
- **output_folder** — Где сохранять захваченные изображения/метки
- **data_folder** — Папка для инструмента разметки и разделения

#### **⚙️ Параметры обучения по умолчанию**
Эти значения отображаются как стандартные в GUI вкладки Training:

| Параметр | По умолч. | Диапазон | Цель |
|----------|-----------|----------|------|
| **epochs** | 50 | 10-500 | Итерации обучения |
| **imgsz** | 640 | 320-1280 | Размер входного изображения |
| **batch** | 16 | 1-128 | Образцов за шаг |
| **optimizer** | SGD | SGD/Adam/AdamW | Тип градиентного спуска |
| **lr0** | 0.01 | 0.0001-0.1 | Learning rate |
| **patience** | 15 | 5-50 | Ранняя остановка |
| **mosaic** | 1.0 | 0-1 | Мультимасштабное обучение |
| **mixup** | 0.1 | 0-1 | Вероятность смешивания |
| **amp** | true | - | Automatic Mixed Precision |

#### **🎨 Настройки UI**
- **font_size** — Размер шрифта GUI (8-20)
- **theme** — Цветовая схема (light_cyan, dark_blue и т.д.)

---

## 🎬 **Конфиг #2: `configs/configStreamCut.json`** — StreamCut

Управляет pipeline обработки VOD: **Скачивание → Разбиение → Инференс → Сохранение**

<details>
<summary><b>📋 Нажмите чтобы развернуть JSON пример</b></summary>

### **Основные параметры**

```json
{
  "video_sources": [
    "https://www.twitch.tv/videos/2522936875",
    "https://www.twitch.tv/videos/2524662899"
  ],
  "use_selected_only": true,
  
  "raw_stream_folder": "stream/raw_streams",
  "chunks_folder": "stream/chunks",
  "output_folder": "stream/dataset",
  
  "model_path": "models/sunxds_0.7.6.pt",
  "classes": ["player", "head"],
  "detection_threshold": 0.7,
  "time_interval": 3,
  
  "max_download_workers": 3,
  "split_workers": 4,
  "process_workers": 8,
  "save_workers": 2,
  
  "ffmpeg_path": "utils/ffmpeg/bin/ffmpeg.exe",
  "download_archive": "stream/downloaded.txt",
  "pause_after_download": true
}
```

</details>

### **Объяснение параметров**

#### **🎥 Источники видео**
- **video_sources** — Список всех URL Twitch VOD для обработки
- **use_selected_only** — Если true, обрабатывать только выбранные (через GUI)

#### **📁 Папки и пути**
- **raw_stream_folder** — Где хранятся скачанные VOD
- **chunks_folder** — Временная папка для сегментов видео
- **output_folder** — Итоговое расположение датасета (images/ + labels/)
- **ffmpeg_path** — Путь к ffmpeg исполняемому файлу

#### **🤖 Детекция и модель**
- **model_path** — YOLO модель для детекции
- **classes** — Какие объекты сохранять (остальные игнорируются)
- **detection_threshold** — Мин. уверенность для сохранения кадра (0-1)
- **time_interval** — Проверять каждый N-й кадр (3 = ~10 проверок/сек при 30fps)

#### **Рабочие потоки** (Самое важное!)

| Воркер | Цель | Рекомендация |
|--------|------|-------------|
| **max_download_workers** | Сколько видео скачиваются одновременно | 2-3 (макс 3, чтобы не забанили) |
| **split_workers** | Потоки FFmpeg для разбиения видео | Количество ядер CPU |
| **process_workers** | GPU потоки для инференса YOLO (каждый извлекает кадры на основе интервала) | 1-4 (зависит от GPU) |
| **save_workers** | Потоки дисковых I/O для сохранения кадров/меток | 1-2 |

**Гайд по тюнингу:**
- Больше download workers = быстрее (но риск бана если >3)
- Больше split workers = используйте все ядра CPU
- Больше process workers = лучше использование GPU (но больше VRAM)
- Больше save workers = редко нужно (I/O обычно не узкое место)

#### **Другие настройки**
- **download_archive** — Файл отслеживания скачанных VOD (избегает перескачивания)
- **pause_after_download** — Спросить перед обработкой после скачивания
- **time_interval** — Частота детекции:
  - `1` = Каждый кадр (полно но медленно)
  - `3-5` = Оптимально (6-10 проверок/сек при 30fps)
  - `30` = Раз в секунду

---

## ⚠️ **Типичные проблемы конфигурации**

| Проблема | Причина | Решение |
|----------|--------|---------|
| Объекты не детектируются | Неверный путь модели или порог слишком высокий | Проверьте путь, снизьте порог до 0.3-0.4 |
| Недостаточно памяти | Batch слишком большой или process_workers много | Снизьте batch или process_workers |
| Медленный сбор данных | Интервал слишком короткий или модель большая | Увеличьте save_interval до 5+ секунд |
| Медленный StreamCut | Недостаточно воркеров | Увеличьте split_workers и process_workers (если железо позволяет) |
| Бан Twitch | Слишком много download workers | Держите max_download_workers ≤ 3 |
| Метки без классов | Неверный class_map | Проверьте индексы классов модели |

---

## 💡 **Рекомендуемые пресеты**

### **Быстрой сбор данных (Скорость)**
```json
{
  "save_interval": 1,
  "detection_threshold": 0.3,
  "crop_size": 0.9,
  "batch": 32
}
```

### **Высокое качество (Точность)**
```json
{
  "save_interval": 5,
  "detection_threshold": 0.7,
  "crop_size": 0.6,
  "epochs": 100,
  "patience": 25
}
```

### **StreamCut на слабой GPU (GTX 1060)**
```json
{
  "process_workers": 1,
  "split_workers": 2,
  "max_download_workers": 2,
  "time_interval": 30,
  "batch": 8
}
```

### **StreamCut на мощной GPU (RTX 3080+)**
```json
{
  "process_workers": 8,
  "split_workers": 8,
  "max_download_workers": 3,
  "time_interval": 3,
  "batch": 32
}
```

---

## 🔗 **Связи между конфигами**

```
config.json
├─ Data Collection → Использует model_path для детекции
├─ Label Tool → Использует output_folder + classes
├─ Dataset Split → Читает из data_folder, пишет в split location
├─ Training Tab → Использует train_defaults как стартовые значения
└─ UI → Использует ui.font_size и ui.theme

configStreamCut.json
├─ StreamCut Tool → Использует все параметры
└─ Advanced Settings → Тюнится пользователем через конфиг
```

---

## 🎯 **Пошаговые примеры конфигурации**

### **Сценарий 1: Быстрый контент-криэйтер (Valorant)**
**Цель:** Создать детектор ранга за 2 часа

1. **config.json:**
   ```json
   {
     "model_path": "models/yolov8s.pt",
     "classes": ["rank_banner", "agent"],
     "detection_threshold": 0.4,
     "save_interval": 1,
     "grabber": {"width": 640, "height": 640, "crop_size": 0.7},
     "train_defaults": {
       "epochs": 30,
       "batch": 32,
       "imgsz": 640
     }
   }
   ```

2. **Процесс:**
   - Захватываем экран 30 минут (~1500 изображений)
   - Проверяем 50 случайных образцов в label tool (5 мин)
   - Обучаем 30 эпох (30 мин на GPU)
   - Экспортируем и тестируем

### **Сценарий 2: Высокое качество - StreamCut из 10 VOD**
**Цель:** Максимальное качество датасета из Twitch

1. **configStreamCut.json:**
   ```json
   {
     "detection_threshold": 0.75,
     "time_interval": 5,
     "max_download_workers": 2,
     "split_workers": 4,
     "process_workers": 4,
     "pause_after_download": true
   }
   ```

2. **Процесс:**
   - Добавляем 10 VOD ссылок в video_sources
   - Запускаем обработку (~2-3 часа)
   - Проверяем метки в label tool
   - Корректируем классы если нужно

### **Сценарий 3: ML-исследование с итерацией**
**Цель:** Быстрая итерация на датасете

1. **config.json фокус на:**
   ```json
   {
     "save_interval": 3,
     "output_folder": "dataset_output/experiment_1",
     "train_defaults": {
       "epochs": 50,
       "imgsz": 416,
       "batch": 8
     }
   }
   ```

2. **Рабочий процесс:**
   - Собираем базовый датасет
   - Обучаем v1
   - Анализируем ошибки
   - Собираем сложные примеры
   - Обучаем v2 с новыми данными
   - Сравниваем метрики

---

## 🔧 **Контрольный список отладки**

### **Проблемы со сбором данных**

**В: Объекты не детектируются при захвате**
- [ ] Файл модели действительно есть? (`models/` папка)
- [ ] Порог слишком высокий? (попробуйте 0.3)
- [ ] Вы пытаетесь детектировать правильные объекты?
- [ ] Разрешение экрана минимум 640x480?
- **Решение:** Снизьте порог → 0.3, проверьте модель, тестируйте известные объекты

**В: Захват очень медленный**
- [ ] save_interval слишком короткий? (попробуйте 3-5)
- [ ] Модель слишком большая? (используйте yolov8n)
- [ ] crop_size слишком большой? (0.8 или меньше)
- [ ] GPU используется? (проверьте CUDA установку)
- **Решение:** Увеличьте save_interval, используйте меньше модель

**В: Метки некорректны**
- [ ] class_map совпадает с порядком классов модели?
- [ ] Порог ловит ложные срабатывания? (повысьте до 0.6+)
- [ ] Bounding box в правильном формате? (нормализованный 0-1)
- **Решение:** Проверьте классы, используйте label tool для исправления

### **Проблемы с обучением**

**В: Обучение падает с CUDA out of memory**
- Уменьшить batch: `"batch": 8` (от 16)
- Уменьшить размер: `"imgsz": 416` (от 640)
- Очистить старые run: Удалить `runs/` папку

**В: Точность модели низкая**
- Не хватает данных? Собрать 2000+ минимум
- Дисбаланс классов? Убедитесь что классы сбалансированы
- Плохие метки? Проверьте 100+ образцов
- Неправильный LR? Попробуйте `"lr0": 0.001`

**В: Обучение очень медленное**
- Используйте меньше модель: `yolov8n`
- Уменьшите размер: `"imgsz": 416`
- Увеличьте batch если VRAM позволяет
- Проверьте GPU: Посмотрите утилизацию в диспетчере задач

### **Проблемы StreamCut**

**В: Видео не скачиваются**
- [ ] URL правильные Twitch VOD ссылки?
- [ ] Есть интернет?
- [ ] yt-dlp обновлен? (`pip install -U yt-dlp`)
- [ ] Workers > 3? (уменьшите чтобы избежать бана)
- **Решение:** Обновите yt-dlp, проверьте ссылки

**В: Twitch rate limit / бан**
- [ ] max_download_workers > 3?
- [ ] Качаете слишком много быстро?
- **Решение:** Установите `max_download_workers: 2`, подождите

**В: StreamCut очень медленный**
- Увеличьте split_workers равно число ядер CPU
- Увеличьте process_workers (если есть GPU)
- Увеличьте time_interval до 5-10
- Используйте слабую модель (yolov8n)

**В: Нет меток из StreamCut**
- [ ] model_path правильный?
- [ ] Классы в configStreamCut.json правильные?
- [ ] Порог не слишком высокий? (попробуйте 0.3)
- [ ] В кадрах действительно ваши объекты?
- **Решение:** Проверьте порог и модель, тестируйте на образцах

---

## 📊 **Оптимизация производительности**

### **Для сбора данных (Screen Capture)**

| Узкое место | Решение | Результат |
|-----------|---------|-----------|
| Медленная детекция | Меньше модель (yolov8n) | ~3x быстрее, чуть менее точно |
| Медленное сохранение | Увеличить save_interval | ~2x быстрее, меньше кадров |
| Много RAM | Уменьшить crop_size (0.6 вместо 0.8) | Ниже разрешение, но быстрее |

### **Для обучения**

| Узкое место | Решение | Результат |
|-----------|---------|-----------|
| Out of memory | Снизить batch 32→16→8 | Медленнее обучение, но работает |
| Медленное обучение | Снизить imgsz 640→416 | ~4x быстрее, небольшая потеря точности |
| GPU медленный | Меньше модель (n/s вместо m/l) | Намного быстрее, менее точно |

### **Для StreamCut**

| Узкое место | Решение | Результат |
|-----------|---------|-----------|
| Медленное скачивание | max_download_workers: 3 (макс!) | ~3x быстрее, риск бана |
| Медленное разделение | split_workers: [число ядер] | Линейное ускорение |
| Медленная инференция | time_interval: 10-30 | ~2-5x быстрее, меньше кадров |
| Медленное сохранение | Обычно не узкое место | - |

---

## 🎨 **Работа с разными YOLO моделями**

### **Руководство выбора модели**

| Модель | Скорость | Точность | Память | Применение |
|--------|----------|----------|--------|-----------|
| yolov8n | ⚡⚡⚡ | ⭐⭐⭐ | 2GB | Real-time, слабое железо |
| yolov8s | ⚡⚡ | ⭐⭐⭐⭐ | 4GB | Сбалансировано, большинство |
| yolov8m | ⚡ | ⭐⭐⭐⭐⭐ | 8GB | Нужна точность |
| yolov12x | 🐌 | ⭐⭐⭐⭐⭐ | 24GB | Исследования, максимум |

### **Смена моделей в конфигурации**

```json
{
  "model_path": "models/yolov8n.pt",  // Меняем это
  "classes": ["player", "weapon"],     // Убедитесь что совпадает!
  "detection_threshold": 0.5
}
```

**Важно:** Каждая модель имеет:
- Разные индексы классов (что означает класс 0, 1, 2)
- Разную калибровку конфиденса
- Разный баланс скорость/точность

---

## 💾 **Экспорт и развертывание моделей**

### **После обучения**

Лучшая модель сохраняется: `runs/detect/train/weights/best.pt`

### **Экспорт в ONNX** (для бенчмаркинга)
```
Из меню Tools → Export Model → Выберите папку run → Выберите ONNX формат
```
Создает `.onnx` файл для кроссплатформенной инференции

### **Использование обученной модели**

1. Скопируйте `best.pt` в `models/` папку
2. Обновите config.json: `"model_path": "models/your_model.pt"`
3. Используйте в:
   - Захвате экрана для сбора данных
   - StreamCut для майнинга VOD
   - Label tool для проверки

---

1. **Всегда делайте бэкап config.json перед крупными изменениями**
2. **Используйте GUI для открытия доступных опций** (конфиги хранят только установленное)
3. **Формат Twitch cookies:** Должен быть Netscape format (экспортируйте из браузер-расширения)
4. **model_path в config.json не влияет на обучение** — Обучение использует модель выбранную в GUI
5. **time_interval в StreamCut:** Если сомневаетесь, используйте 3-5 для хорошего баланса

---

## 🆘 **Получение помощи**

- Посмотрите README.md для обзора workflow
- Смотрите HELP.md для английской документации
- Проверьте типичные проблемы выше перед сообщением об ошибках

---

<div align="center">

**Конфигурационные файлы — это расширенные настройки. Большинство пользователей контролируют всё через GUI!**

</div>

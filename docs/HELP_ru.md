<div align="center">

# ⚙️ **YDS — Справка по конфигурации**

Краткое руководство по всем параметрам `config.json` и `configStreamCut.json`.

<p align="center">
  <a href="HELP.md">🇺🇸 English</a> | <a href="HELP_ru.md">🇷🇺 <b>Русский</b></a>
</p>

</div>

## 📌 **Быстрая навигация**

- [Где лежат конфиги](#где-лежат-файлы-конфигурации)
- [Структура выходных данных](#структура-выходных-данных-yolo-формат)
- [Быстрый старт](#быстрый-старт--выберите-вашу-задачу)
- [Справочник config.json](#справочник-configjson)
- [Справочник configStreamCut.json](#справочник-configstreamcutjson)
- [Важные зависимости](#важные-зависимости--подводные-камни)
- [Пресеты](#пресеты--готовые-конфиги)
- [Устранение проблем](#устранение-проблем)
- [Оптимизация производительности](#оптимизация-производительности)

---

## 📂 **Где лежат файлы конфигурации**

```
YolovTrainGui/
├── configs/
│   ├── config.json              # Основной конфиг (захват экрана, обучение, UI)
│   └── configStreamCut.json     # Конфиг StreamCut (Twitch VOD, воркеры)
```

| Файл | Назначение | Изменяется |
|------|-----------|-------------|
| **config.json** | Захват экрана, значения по умолчанию для обучения, UI | GUI или JSON редактор |
| **configStreamCut.json** | Скачивание VOD с Twitch, обработка, параллельные потоки | GUI или JSON редактор |

---

## 📦 **Структура выходных данных (YOLO формат)**

### **Выход Screen Capture**
```
output_folder/ (по умолчанию: "dataset_output")
├── images/
│   ├── frame_001.png
│   ├── frame_002.png
│   └── ...
└── labels/
    ├── frame_001.txt
    ├── frame_002.txt
    └── ...
```

### **Выход StreamCut**
```
stream/dataset/ (или output_folder в configStreamCut.json)
├── images/
│   ├── chunk_0_frame_100.png
│   └── ...
└── labels/
    ├── chunk_0_frame_100.txt
    └── ...
```

### **Формат меток (YOLO стандарт)**
Каждый файл `.txt` содержит одну строку на каждое обнаружение:
```
<class_id> <x_center> <y_center> <width> <height>
```
- `class_id` — индекс в массиве `classes` (0, 1, 2, ...)
- `x_center, y_center, width, height` — нормализовано к 0–1 (независимо от размера)

**Пример:**
```
0 0.5 0.5 0.3 0.4
1 0.2 0.7 0.15 0.2
```
Значит: класс 0 в центре, класс 1 слева-снизу.

---

## 🚀 **Быстрый старт — выберите вашу задачу**

### **"Хочу собирать данные с экрана"**
Отредактируйте `config.json`:
- `model_path` → какую YOLO модель использовать
- `detection_threshold` → уровень уверенности (пробуйте 0.3–0.5)
- `save_interval` → минимум секунд между сохранениями
- `grabber.crop_size` → обрезка экрана (0.6 = 60%)

**Перейти к:** [Параметры сбора данных](#-сбор-данных)

### **"Хочу скачивать и обрабатывать VOD с Twitch"**
Отредактируйте `configStreamCut.json`:
- `video_sources` → добавьте URL Twitch
- `model_path` → какую YOLO модель использовать
- `time_interval` → инференс каждый N-й кадр (5 = каждый 5-й)
- `process_workers` → параллельный инференс (4 = 4 потока)

**Перейти к:** [Параметры StreamCut](#-загрузка--обработка)

### **"Мои обнаружения неправильные"**
Проверьте:
- `classes` → правильные названия классов?
- `class_map` → соответствует ли модели?
- `detection_threshold` → слишком высокий? пробуйте 0.2–0.3

**Перейти к:** [Важные зависимости](#важные-зависимости--подводные-камни)

### **"GPU работает медленно"**
Попробуйте:
- Screen capture: используйте `yolov8n.pt` вместо большей модели
- StreamCut: уменьшите `process_workers` или увеличьте `time_interval`

**Перейти к:** [Оптимизация производительности](#оптимизация-производительности)

---

## ⚙️ **Справочник config.json**

### **📸 Сбор данных**

#### `model_path`
- **Тип:** string (путь к файлу)
- **По умолчанию:** `"models/yolov8n.pt"`
- **Используется в:** Screen Capture
- **Значение:** Какую YOLO модель использовать для обнаружения объектов
- **Совет:** Меньше модель = быстрее, но менее точно. Используйте `yolov8n` для скорости, `yolov8m` для точности.

#### `detection_threshold`
- **Тип:** float (0.0–1.0)
- **По умолчанию:** `0.5`
- **Используется в:** Screen Capture (влияет на то, что сохраняется)
- **Значение:** Минимальная уверенность для сохранения обнаружения
- **Совет:** Если ничего не сохраняется, пробуйте `0.3–0.4`. Если много ложных срабатываний, поднимите до `0.6–0.7`.

#### `save_interval`
- **Тип:** integer (секунды)
- **По умолчанию:** `2`
- **Используется в:** Screen Capture
- **Значение:** Минимальная задержка между сохранениями (предотвращает дублирование кадров)
- **Совет:** Увеличьте до `5–10`, если датасет слишком большой. Уменьшите до `1` для плотной выборки.

---

### **🖼️ Захват экрана (grabber)**

#### `grabber.crop_size`
- **Тип:** float (0.0–1.0)
- **По умолчанию:** `0.8`
- **Используется в:** Screen Capture
- **Значение:** Процент центральной обрезки (0.8 = обрезка до 80% минимального размера экрана)
- **Совет:** Меньшие значения исключают края экрана. Используйте `0.6–0.8` для игр.

#### `grabber.width` / `grabber.height`
- **Тип:** integer (пиксели)
- **По умолчанию:** `1920` / `1080`
- **Используется в:** Screen Capture
- **Значение:** Разрешение захвата
- **Совет:** Больше = больше деталей, но медленнее. Используйте 1280×720 для скорости, 1920×1080 для деталей.

---

### **📁 Папки и пути**

#### `output_folder`
- **Тип:** string (путь)
- **По умолчанию:** `"dataset_output"`
- **Используется в:** Screen Capture
- **Значение:** Где сохранять захваченные `images/` и `labels/`

#### `data_folder`
- **Тип:** string (путь)
- **По умолчанию:** `"datasets"`
- **Используется в:** Split Dataset, Label Verification
- **Значение:** Папка датасета по умолчанию для всех инструментов

#### `label_data_folder`
- **Тип:** string (путь)
- **По умолчанию:** `""` (используется `data_folder`)
- **Используется в:** Инструмент Label Verification
- **Значение:** Какую папку открывать в инструменте разметки

#### `last_data_yaml`
- **Тип:** string (путь)
- **По умолчанию:** `""`
- **Используется в:** Training GUI
- **Значение:** Последний выбранный `data.yaml` (автоматически, только чтение)

---

### **🏷️ Классы**

#### `classes`
- **Тип:** array of strings
- **По умолчанию:** `["class1", "class2"]`
- **Используется в:** Screen Capture, Label Verification, Training
- **Значение:** Список названий классов объектов. **Порядок имеет значение!** Индекс в метках = позиция в массиве.
- **Совет:** Если порядок классов не совпадает с выходом модели, используйте `class_map` для переопределения.

#### `class_map`
- **Тип:** object (пары ключ-значение)
- **По умолчанию:** `{}`
- **Используется в:** Screen Capture
- **Значение:** Соответствие между индексами класса модели и классами датасета
- **Пример:** `{"0": "player", "7": "head"}` = класс модели 0 → сохранить как "player", класс 7 → сохранить как "head"
- **Совет:** Генерируйте с помощью кнопки GUI "Show Classes from Model".

---

### **⚙️ Значения по умолчанию для обучения**

#### `train_defaults`
- **Тип:** object
- **По умолчанию:** см. ниже
- **Используется в:** Training GUI (заполняет значения по умолчанию)
- **Значение:** Предзаполненные параметры обучения (все переопределяются в GUI)

Ключевые параметры:
- `epochs` (int) — итерации обучения
- `imgsz` (int) — размер входного изображения (320, 416, 640)
- `batch` (int) — размер батча (уменьшите, если нехватает памяти GPU)
- `patience` (int) — ранняя остановка (стоп если нет улучшения N эпох)
- `amp` (bool) — Автоматическая смешанная точность (быстрее на современных GPU)
- `mosaic`, `mixup`, `fliplr`, `hsv_h`, и т.д. — параметры аугментации

---

### **🎨 Настройки UI**

#### `ui.font_size`
- **Тип:** integer (8–14)
- **По умолчанию:** `10`
- **Используется в:** Внешний вид GUI
- **Значение:** Размер шрифта для всех окон GUI

#### `ui.theme`
- **Тип:** string
- **По умолчанию:** `"light_blue"`
- **Используется в:** Внешний вид GUI
- **Значение:** Название темы Qt-material (light_blue, dark_teal, light_cyan, и т.д.)

#### `ui.show_capture_window`
- **Тип:** boolean
- **По умолчанию:** `true`
- **Используется в:** Screen Capture
- **Значение:** Показывать окно предпросмотра OpenCV во время захвата
- **Совет:** Установите `false`, если предпросмотр замедляет работу.

---

## 🎬 **Справочник configStreamCut.json**

### **📥 Загрузка и источники**

#### `video_sources`
- **Тип:** array of strings (URL)
- **По умолчанию:** `[]`
- **Используется в:** Список GUI StreamCut
- **Значение:** Все URL VOD с Twitch для потенциальной загрузки
- **Пример:** `["https://www.twitch.tv/videos/1234567890"]`

#### `selected_video_sources`
- **Тип:** array of integers (индексы)
- **По умолчанию:** `[]`
- **Используется в:** StreamCut
- **Значение:** Какие URL из `video_sources` выбраны (используется только если `use_selected_only: true`)
- **Пример:** `[0, 2]` = обрабатывать 1-й и 3-й URL

#### `use_selected_only`
- **Тип:** boolean
- **По умолчанию:** `false`
- **Используется в:** StreamCut
- **Значение:** Если true, обрабатывать только `selected_video_sources`. Если false, обрабатывать все `video_sources`.

#### `download_quality`
- **Тип:** string
- **По умолчанию:** `"720p"`
- **Используется в:** Загрузка StreamCut
- **Значение:** Максимальное качество для загрузки (передаётся в yt-dlp)
- **Варианты:** `"best"`, `"720p"`, `"480p"`, `"360p"`

#### `twitch_cookies_path`
- **Тип:** string (путь)
- **По умолчанию:** `"Cookies/cookies.txt"`
- **Используется в:** Загрузка StreamCut
- **Значение:** Файл cookies в формате Netscape (требуется для приватных или age-gated VOD)

#### `raw_stream_folder`
- **Тип:** string (путь)
- **По умолчанию:** `"stream/raw_streams"`
- **Используется в:** StreamCut
- **Значение:** Где загруженные VOD хранятся

#### `download_archive`
- **Тип:** string (путь)
- **По умолчанию:** `"stream/.download_archive"`
- **Используется в:** StreamCut
- **Значение:** Файл отслеживающий загруженные URL. Используется функцией **Sync** для пропуска уже загруженных VOD.

---

### **🔄 Конвейер обработки**

#### `chunks_folder`
- **Тип:** string (путь)
- **По умолчанию:** `"stream/chunks"`
- **Используется в:** StreamCut
- **Значение:** Временная папка для сегментов `.ts` во время обработки

#### `chunks_per_stream`
- **Тип:** integer
- **По умолчанию:** `10`
- **Используется в:** StreamCut
- **Значение:** На сколько сегментов разбить каждый VOD
- **Совет:** Больше сегментов = больше параллельной обработки, но больше нагрузка на ffmpeg.

#### `ffmpeg_path`
- **Тип:** string (путь)
- **По умолчанию:** `"utils/ffmpeg/bin/ffmpeg.exe"`
- **Используется в:** StreamCut
- **Значение:** Путь к исполняемому файлу ffmpeg (используется для разделения VOD)

#### `output_folder`
- **Тип:** string (путь)
- **По умолчанию:** `"stream/dataset"`
- **Используется в:** StreamCut
- **Значение:** Финальный выход датасета (`images/` и `labels/`)

#### `time_interval`
- **Тип:** integer (кадры)
- **По умолчанию:** `5`
- **Используется в:** StreamCut
- **Значение:** Запускать инференс каждый N-й кадр (5 = проверить каждый 5-й кадр)
- **Совет:** Больше значение = быстрее, но меньше кадров. Меньше = медленнее, но плотнее выборка.

#### `resume_info_file`
- **Тип:** string (путь)
- **По умолчанию:** `"stream/.resume_info"`
- **Используется в:** StreamCut
- **Значение:** Файл отслеживающий какие сегменты обработаны. Используется для возобновления прерванных запусков.
- **Совет:** Удалите этот файл, чтобы переобработать все сегменты.

---

### **🤖 Обнаружение и модель**

#### `model_path`
- **Тип:** string (путь)
- **По умолчанию:** `"models/yolov8n.pt"`
- **Используется в:** Инференс StreamCut
- **Значение:** YOLO модель для обнаружения объектов в кадрах VOD
- **Совет:** Должна существовать перед запуском StreamCut.

#### `detection_threshold`
- **Тип:** float (0.0–1.0)
- **По умолчанию:** `0.3`
- **Используется в:** StreamCut
- **Значение:** Минимальная уверенность для сохранения обнаружения

#### `classes`
- **Тип:** array of strings
- **По умолчанию:** `[]`
- **Используется в:** StreamCut
- **Значение:** Только эти классы сохраняются в выходном датасете
- **Совет:** Если пусто, сохраняются все обнаруженные классы.

---

### **⚡ Параллельные воркеры и производительность**

#### `max_download_workers`
- **Тип:** integer (1–8)
- **По умолчанию:** `2`
- **Используется в:** Загрузка StreamCut
- **Значение:** Параллельные загрузки с Twitch
- **⚠️ Важно:** Держите ≤ 2–3 чтобы избежать ограничений Twitch!

#### `split_workers`
- **Тип:** integer (1–8)
- **По умолчанию:** `4`
- **Используется в:** StreamCut (ffmpeg)
- **Значение:** Параллельное разделение VOD сегментов

#### `process_workers`
- **Тип:** integer (1–8)
- **По умолчанию:** `4`
- **Используется в:** Инференс StreamCut
- **Значение:** Параллельные потоки YOLO инференса
- **Совет:** Увеличьте если GPU недоиспользуется. Уменьшите если CPU на максимуме.

#### `save_workers`
- **Тип:** integer (1–8)
- **По умолчанию:** `4`
- **Используется в:** StreamCut
- **Значение:** Параллельная запись на диск (сохранение images/labels)

#### `pause_after_download`
- **Тип:** boolean
- **По умолчанию:** `true`
- **Используется в:** GUI StreamCut
- **Значение:** Запросить подтверждение перед обработкой после завершения загрузки

---

## ⚠️ **Важные зависимости и подводные камни**

### **1. `model_path` в config.json vs Training**
❌ **Частая ошибка:** Редактирование `model_path` в `config.json` не влияет на вкладку Training.

✅ **Почему:** Training использует модель выбранную в **GUI Training** + `data.yaml`. `config.json` model_path используется **только для обнаружения при захвате экрана**.

### **2. Порядок `classes` = Class ID**
❌ **Частая ошибка:** Изменение порядка классов в середине сбора датасета.

✅ **Почему:** Позиция в массиве `classes` становится ID метки. Если переупорядочить, старые метки не совпадут.

**Решение:** Определите классы заранее и не переупорядочивайте.

### **3. `class_map` и индексы модели**
**Используйте `class_map` когда:** Ваша модель выводит индексы классов (0, 7, 15...) но вы хотите отобразить их на названия классов датасета.

**Как это работает:**
1. Модель выводит индекс класса (например, 7)
2. `class_map` преобразует его в название класса (например, `"7": "head"`)
3. YDS находит "head" в массиве `classes` и получает его позицию (индекс)
4. Эта позиция становится `class_id` в сохраняемой метке

**Пример:**
```json
"classes": ["player", "head", "weapon"],
"class_map": {"0": "player", "7": "head"}
```

Результат:
- Выход модели 0 → отображается в "player" → находится в `classes[0]` → сохраняется как class_id `0`
- Выход модели 7 → отображается в "head" → находится в `classes[1]` → сохраняется как class_id `1`
- Выход модели 15 → не в class_map → игнорируется

### **4. `download_archive` и StreamCut Sync**
**Как работает:** StreamCut отслеживает загруженные URL в `download_archive`. Клик **Sync** пропускает уже загруженные VOD.

**Если Sync не работает:**
1. Проверьте что файл `download_archive` существует
2. Проверьте что `raw_stream_folder` содержит VOD файлы
3. Убедитесь что URL идентичны

### **5. `time_interval` в КАДРАХ, не секундах**
❌ **Частая ошибка:** Установка `time_interval: 30` ожидая 30 секунд.

✅ **Реальность:** `time_interval: 30` = проверять каждый 30-й кадр.

---

## 🔧 **Устранение проблем**

| Проблема | Причина | Решение |
|----------|--------|--------|
| Ничего не сохраняется при захвате | `detection_threshold` слишком высокий | Понизьте до 0.3–0.4 |
| Слишком много ложных срабатываний | `detection_threshold` слишком низкий | Поднимите до 0.6–0.8 |
| Инструмент разметки открывает неправильную папку | `label_data_folder` не установлен | Установите в config.json |
| StreamCut не пропускает загруженные VOD | Файл `download_archive` не доступен или URL не совпадают | Проверьте что файл существует и доступен; верифицируйте формат URL |
| Training игнорирует модель из конфига | Ожидаемое поведение | Training использует модель из селектора GUI, не из конфига |
| Захват экрана медленный | Модель слишком большая | Используйте `yolov8n.pt` вместо `yolov8m.pt` |
| StreamCut медленный | `process_workers` слишком много | Уменьшите до 2–4 |
| Ошибка "rate limit" от Twitch | Слишком много параллельных загрузок | Установите `max_download_workers: 1` |
| Классы не совпадают с моделью | `class_map` отсутствует | Запустите "Show Classes from Model" в GUI |

---

## 📊 **Оптимизация производительности**

| Узкое место | Изменение | Ожидаемый результат |
|-----------|----------|-------------------|
| Память GPU (нехватка памяти) | Уменьшить размер `batch` или использовать `amp: true` | Медленнее, но вмещается в память |
| CPU на максимуме при захвате | Использовать меньшую модель (`yolov8n`) | Быстро, но менее точно |
| GPU недоиспользуется в StreamCut | Увеличить `process_workers` | Лучше использование GPU |
| Загрузка медленная | Увеличить `max_download_workers` (макс 3) | Быстрее, но риск блокировки |
| Датасет слишком большой | Увеличить `save_interval` | Меньше изображений, меньше хранилища |

---

## 🔗 **Связанные материалы**

- [📖 Основной README](../README.md) — Полное руководство пользователя
- [⚙️ Посмотреть текущий config.json](../configs/config.json)
- [🎬 Посмотреть текущий configStreamCut.json](../configs/configStreamCut.json)
